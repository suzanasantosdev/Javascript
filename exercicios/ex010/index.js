// DADOS PRIMITIVOS: NUMBER, UNDEFINED, NULL, BOOLEAN.

const nome = 'Suzana'; // string
const nome1 = "Suzana"; // string
const nome2 = `Suzana`; // string
const num1 = 10; //Number
const num2 = 10.52; // Number
let nomeAluno; // undefined = nao aponta pra local nenhuma na memoria
const sobrenomeAluno = null; // Nulo -> nao aponta pra local nenhuma na memoria
const aprovado = false; // Boolean = true, false (logico)

console.log (typeof num1,num1);